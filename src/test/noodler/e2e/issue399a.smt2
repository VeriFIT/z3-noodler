(set-logic ALL)
(set-info :status sat)
(declare-const x String)
(declare-const y Real)
(assert (= x (str.from_real y 0)))
(assert (< y 2))
(assert (> y 1))
(assert (not (str.contains x ".")))
(check-sat)
(get-model)
